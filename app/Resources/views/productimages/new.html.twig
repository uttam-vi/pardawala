{% extends 'base.html.twig' %}

{% block body %}
    <h1>Productimage creation</h1>

    {{ form_start(form) }}
        <br>
        <div class="form-group">
            <div class="col-md-4">
                {{ form_widget(form) }}<br>
            </div>
    
            <div class="col-sm-10">
                <input type="submit" value="Create" class="btn btn-info"/>
                <a href="{{ path('productimages_index') }}" class="btn btn-success">Back to the list</a>
            </div>
        </div>
    {{ form_end(form) }}

{% endblock %}
