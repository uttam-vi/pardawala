{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-6">
            <div class="content-box-large">
                <div class="panel-heading">
                    <div class="panel-title"><strong>Product Category Edit</strong></div>
                </div>
                <div class="panel-body">
                    {{ form_start(edit_form) }}
{#                            {{ form_widget(form) }}#}
                        <fieldset>
                            <div class="form-group">
                                <label>Name</label>
                                {{ form_widget(edit_form.name) }}
                            </div>
                            <div class="form-group">
                                <label>Image</label>
                                {{ form_widget(edit_form.image) }}<br>
                                <img src="/{{product_images_directory_assert}}/{{ imageCategory.image }}" width="100"><br>
                            </div>
                        </fieldset>
                        <div>
                            <input type="submit" value="Edit" class="btn btn-info"/>
                            <a href="{{ path('imagecategory_index') }}" class="btn btn-default">Back to the list</a>
                        </div>
                    {{ form_end(edit_form) }}
                </div>
            </div>
        </div>
    </div>
                
    {#<h1>Imagecategory edit</h1>

    {{ form_start(edit_form) }}
        <br>
        <div class="form-group">
            <div class="col-md-4">
                {{ form_widget(edit_form) }}<br>
                <img src="/{{product_images_directory_assert}}/{{ imageCategory.image }}" width="100">
            </div>
            <br>
            <div class="col-sm-10">
                <input type="submit" value="Edit" class="btn btn-info"/>
                <a href="{{ path('imagecategory_index') }}" class="btn btn-success">Back to the list</a>
            </div>
        </div>
   
    {{ form_end(edit_form) }}#}
{% endblock %}
